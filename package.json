{
  "name": "survey-jim2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "load:test": "node scripts/run-load-test",
    "load:test:baseline": "node scripts/run-load-test --profile=local-baseline",
    "load:test:full": "node scripts/run-load-test --profile=full"
  }
}
