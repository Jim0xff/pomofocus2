# Models Scaffold

This directory preserves the template location where domain models or entity
definitions would live in the redpacket-server structure.

Current state:

- The working API still uses the SQLite repository in [src/db.js](/tmp/deepflow-assets/openclaw-dev-shared/projects/hackathon-signup12/repo/src/db.js).
- No runtime code imports this directory yet.
- The directory exists as TPLG evidence that the model layer slot is preserved.
