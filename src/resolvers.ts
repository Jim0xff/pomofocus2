/**
 * Template-alignment scaffold for the redpacket-server style resolver layer.
 *
 * No resolver wiring is active yet. The exported object is a placeholder to
 * preserve a compatible repository shape while the current REST handlers remain
 * authoritative.
 */
const templateResolverScaffold = {
  Query: {},
  Mutation: {},
};

module.exports = {
  templateResolverScaffold,
};
